{{#each posts as |post|}}
  <div>
    <div>
      <h2>{{{post.title}}}</h2>
      <h2>Posted by
        {{{post.user.email}}}
        on
        {{formatDate post.date format="MM/DD/YYYY"}}
      </h2>
    </div>
    <div>
      <p>{{post.content}}</p>
    </div>
  </div>
{{/each}}